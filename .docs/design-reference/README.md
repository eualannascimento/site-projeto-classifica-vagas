# Referência de Design

Export estático do modelo visual de referência (antes na raiz como "Gerador de Currículo Gratuito").
Não faz parte da aplicação: serve apenas de consulta para comparar a UI implementada com o design original.

- `Currículo.dc.html` / `Currículo - Mobile.dc.html`: telas de referência (desktop e mobile)
- `screenshots/`: capturas do modelo
- `versao-anterior/`: snapshot anterior do mesmo export (antiga pasta `new/`)
